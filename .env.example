# Copy this file to `.env` and fill values for local development / deployment.
# Secrets
DJANGO_SECRET_KEY=change-me-in-dev
DEBUG=1

# Database (use DATABASE_URL in production)
DATABASE_URL=sqlite:///db.sqlite3

# Gemini / GenAI
GEMINI_API_KEY=

# Storage (S3 / R2 / Spaces)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=
AWS_S3_ENDPOINT_URL=

# Email (example)
EMAIL_HOST=
EMAIL_PORT=
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=webmaster@localhost

# Other
ALLOWED_HOSTS=localhost,127.0.0.1

# Railway deployment (example placeholder)
RAILWAY_API_KEY=
